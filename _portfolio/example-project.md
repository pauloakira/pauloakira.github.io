---
title: "Example Research Project"
excerpt: "Short description of a research project. Replace this entry with a real one."
collection: portfolio
---

Replace this file with a real project. Each project lives as a markdown file in
`_portfolio/`. Useful frontmatter fields:

- `title` — project name
- `excerpt` — one-line summary shown in the listing
- `header.image` — optional banner image (place under `images/`)

Body can include text, equations, code blocks, and figures.
